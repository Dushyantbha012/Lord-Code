AI Coding Agent - Lord Code

A CLI-based conversational agent that lives in your terminal
Connected to an LLM with tool-use/function-calling capabilities
Equipped with tools (read files, write files, execute shell commands, search code)
Aware of project context (codebase structure, git history, etc.)
Operating in an agentic loop (think → act → observe → repeat)